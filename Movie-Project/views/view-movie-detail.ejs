<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        <%= movie.title %> - BookMyShow
    </title>

    <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">

    <style>
        body {
            background-color: #f0f0f0;
            padding-bottom: 105px;
        }

        .movie-banner {
            height: 480px;
            background: linear-gradient(to bottom,
                    rgba(0, 0, 0, 0.4),
                    rgba(0, 0, 0, 0.8)),
                url('/uploads/<%= movie.image %>') center/cover no-repeat;
            color: white;
            display: flex;
            align-items: center;
        }

        .poster-box img {
            border-radius: 15px;
            width: 100%;
        }

        .section-title {
            font-weight: 600;
            margin-top: 40px;
        }

        .badge {
            font-size: 12px;
        }

        .btn-red {
            background-color: #da253d;
            color: #ffffff;
            padding: 10px 90px;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        .btn-red:hover {
            background-color: #f0314b;
        }
    </style>
</head>

<body>
    <!-- Navbar -->
    <header>
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
                <a class="navbar-brand" href="/"><img src="/images/logo.png" alt="logo" width="150" /></a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <form class="d-flex mx-auto w-50" role="search">
                        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" />
                        <button class="btn btn-outline-danger" type="submit">
                            Search
                        </button>
                    </form>
                    <ul class="navbar-nav mb-2 mb-lg-0 gap-3">
                        <li class="nav-item">
                            <a class="btn btn-danger" aria-current="page" href="/movies/addMovie">Add Movie</a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-outline-danger" href="/movies/viewAllMovie">View All Movie</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <!-- main section -->
    <main>
        <!-- Movie Banner -->
        <section class="movie-banner">
            <div class="container">
                <div class="row align-items-end">
                    <div class="col-md-3 poster-box">
                        <img src="/uploads/<%= movie.image %>" alt="<%= movie.title %>" />
                    </div>
                    <div class="col-md-9 text-white">
                        <h1 class="fw-bold">
                            <%= movie.title %>
                        </h1>
                        <p class="badge text-bg-secondary mb-1">
                            <strong class="text-warning fw-bold">&starf;</strong>
                            <%= movie.rating %> / 10
                        </p>
                        <p class="mb-1">

                        </p>
                        <p class="mb-1">
                            <%= movie.duration %> <span class="mx-1"> • </span>
                                <%= movie.genre.join(' , ') %> <span class="mx-1"> • </span> <%= movie.language.join(' , ') %> </p>

                        <a class="btn btn-danger btn-red mt-3" href="#">Book Tickets</a>
                    </div>
                </div>
            </div>
        </section>

      <!-- Movie Detail Section -->
      <div class="container">
        <div class="row mt-5">
          <div class="col-md-8">
            <h3 class="section-title">About the Movie</h3>
            <p class="text-muted"><%= movie.desc %></p>
          </div>
        </div>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
        crossorigin="anonymous"></script>
  </body>
</html>