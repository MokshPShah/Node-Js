<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Books | Book Store</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <div class="container">
        <h1 class="heading">Book Store - All Books</h1>
        
        <table class="book-list">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Author</th>
                    <th>ISBN</th>
                    <th>Price</th>
                    <th>Language</th>
                    <th>Description</th>
                </tr>
            </thead>
            <tbody>
                <% books.forEach((book) => { %>
                    <tr>
                        <td><%= book.title %></td>
                        <td><%= book.author %></td>
                        <td><%= book.isbn %></td>
                        <td><%= book.price %></td>
                        <td><%= book.language %></td>
                        <td><%= book.desc %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>

        <a href="/add">Add another book</a>
    </div>
</body>
</html>
