<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel | NewsChaser</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .user-table { width: 100%; border-collapse: collapse; background: white; border-radius: 8px; overflow: hidden; box-shadow: var(--shadow); }
        .user-table th, .user-table td { padding: 1rem; text-align: left; border-bottom: 1px solid var(--border); }
        .user-table th { background: #f1f5f9; font-weight: 600; }
        .badge-user { background: #3b82f6; color: white; padding: 4px 8px; border-radius: 12px; font-size: 0.8rem; }
        .badge-admin { background: #ef4444; color: white; padding: 4px 8px; border-radius: 12px; font-size: 0.8rem; }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>

    <main class="container">
        <header class="page-header">
            <h1 class="page-title">Admin Dashboard</h1>
        </header>

        <section>
            <h3 style="margin-bottom: 1rem;">Registered Users</h3>
            <table class="user-table">
                <thead>
                    <tr>
                        <th>Username</th>
                        <th>Role</th>
                        <th>Articles Posted</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% if(users.length > 0) { %>
                        <% users.forEach(u => { %>
                            <tr>
                                <td><%= u.username %></td>
                                <td>
                                    <span class="<%= u.role === 'admin' ? 'badge-admin' : 'badge-user' %>">
                                        <%= u.role.toUpperCase() %>
                                    </span>
                                </td>
                                <td><%= u.articles.length %></td>
                                <td>
                                    <form action="/admin/delete-user/<%= u._id %>" method="POST" onsubmit="return confirm('WARNING: This will delete the user AND all their articles. Proceed?');">
                                        <button type="submit" class="btn" style="background-color: #fee2e2; color: #ef4444; padding: 0.4rem 0.8rem; font-size: 0.85rem;">
                                            Delete User
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <% }) %>
                    <% } else { %>
                        <tr><td colspan="4" style="text-align:center;">No other users found.</td></tr>
                    <% } %>
                </tbody>
            </table>
        </section>
    </main>

    <%- include('partials/footer') %>
</body>
</html>