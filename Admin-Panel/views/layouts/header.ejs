<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | Nexus Admin
    </title>
    <link rel="shortcut icon" href="/images/logo.png" type="image/x-icon">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }

        /* Hide scrollbar for Chrome, Safari and Opera */
        .no-scrollbar::-webkit-scrollbar {
            display: none;
        }

        .no-scrollbar {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }
    </style>
</head>

<body class="bg-slate-50 text-slate-800 antialiased">

    <div class="flex h-screen w-full">
        <aside class="w-64 bg-white border-r border-slate-200 flex flex-col hidden md:flex z-20 fixed h-full">
            <a href="/dashboard">
                <div class="h-16 flex items-center px-6 border-b border-slate-100">
                    <img src="/images/logo.png" alt="logo" width="45">
                    <span class="text-xl font-bold text-slate-800 tracking-tight">Nexus<span
                            class="text-indigo-600">Admin</span>
                    </span>
                </div>
            </a>
            <nav class="flex-1 overflow-y-auto py-4 px-3 space-y-1">
                <a href="/dashboard"
                    class="nav-link flex items-center px-3 py-2.5 text-slate-600 rounded-lg hover:bg-slate-50 hover:text-indigo-600 transition-colors group">
                    <i data-lucide="layout-dashboard" class="w-5 h-5 mr-3 group-hover:text-indigo-600"></i>
                    <span class="font-medium">Dashboard</span>
                </a>
                <div class="space-y-1">
                    <button id="dropdown-btn"
                        class="w-full flex items-center justify-between px-3 py-2.5 text-slate-600 rounded-lg hover:bg-slate-50 hover:text-indigo-600 transition-colors group">
                        <div class="flex items-center">
                            <i data-lucide="users" class="w-5 h-5 mr-3 group-hover:text-indigo-600"></i>
                            <span class="font-medium">Admin Management</span>
                        </div>
                        <i data-lucide="chevron-down" id="dropdown-icon"
                            class="w-4 h-4 transition-transform duration-200"></i>
                    </button>
                    <div id="dropdown-menu" class="hidden pl-10 space-y-1 pt-1">
                        <a href="/add-admin"
                            class="nav-link block px-3 py-2 text-sm text-slate-500 rounded-md hover:text-indigo-600 hover:bg-indigo-50"
                            data-page="add-admin.html">Add New Admin</a>
                        <a href="/view-admin"
                            class="nav-link block px-3 py-2 text-sm text-slate-500 rounded-md hover:text-indigo-600 hover:bg-indigo-50"
                            data-page="view-admin.html">View All Admins</a>
                    </div>
                </div>
            </nav>
            <div class="p-4 border-t border-slate-100 relative">

                <div id="user-dropdown"
                    class="hidden absolute bottom-full left-4 right-4 mb-2 bg-white rounded-xl shadow-xl border border-slate-100 z-50 overflow-hidden transition-all duration-200 origin-bottom-left">

                    <div class="py-1 border-b border-slate-50">
                        <a href="#"
                            class="flex items-center px-4 py-2.5 text-sm text-slate-700 hover:bg-slate-50 hover:text-indigo-600 transition-colors">
                            <i data-lucide="user" class="w-4 h-4 mr-3 text-slate-400"></i>
                            My Profile
                        </a>
                        <a href="#"
                            class="flex items-center px-4 py-2.5 text-sm text-slate-700 hover:bg-slate-50 hover:text-indigo-600 transition-colors">
                            <i data-lucide="settings" class="w-4 h-4 mr-3 text-slate-400"></i>
                            Account Settings
                        </a>
                        <a href="#"
                            class="flex items-center px-4 py-2.5 text-sm text-slate-700 hover:bg-slate-50 hover:text-indigo-600 transition-colors">
                            <i data-lucide="credit-card" class="w-4 h-4 mr-3 text-slate-400"></i>
                            Billing & Plan
                        </a>
                    </div>

                    <div class="py-1 border-b border-slate-50">
                        <a href="#"
                            class="flex items-center px-4 py-2.5 text-sm text-slate-700 hover:bg-slate-50 hover:text-indigo-600 transition-colors">
                            <i data-lucide="help-circle" class="w-4 h-4 mr-3 text-slate-400"></i>
                            Help & Support
                        </a>
                    </div>

                    <div class="py-1 bg-slate-50">
                        <button onclick="alert('Logging out...')"
                            class="flex w-full items-center px-4 py-2.5 text-sm text-red-600 hover:bg-red-50 transition-colors">
                            <i data-lucide="log-out" class="w-4 h-4 mr-3"></i>
                            Sign Out
                        </button>
                    </div>
                </div>

                <button id="user-menu-btn"
                    class="flex items-center w-full hover:bg-slate-50 p-2 rounded-lg transition-colors group text-left">
                    <img src="<%= admin.avatar %>"
                        class="h-9 w-9 rounded-full shadow-sm ring-2 ring-transparent group-hover:ring-indigo-100 transition-all">
                    <div class="ml-3 flex-1 overflow-hidden">
                        <p class="text-sm font-semibold text-slate-700 truncate"><%= admin.name %></p>
                        <p class="text-xs text-slate-500 truncate"><%= admin.email %></p>
                    </div>
                    <i data-lucide="chevron-up" id="user-chevron"
                        class="w-4 h-4 text-slate-400 transition-transform duration-200"></i>
                </button>
            </div>
        </aside>